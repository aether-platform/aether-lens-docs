---
const features = [
  {
    title: "NPU Power",
    description:
      "ローカル NPU でログを 1/100 に圧縮。トークンコストを劇的に抑えつつ、オフラインでも高度な解析を実現します。",
    icon: "🧠",
    color: "#10b981",
  },
  {
    title: "VTI Reading",
    description:
      "差分ではなく「意図」を解析。AIが最適な検証シナリオを自動構成し、あなたの開発リズムを自律的に守ります。",
    icon: "👁️",
    color: "var(--color-aether-primary)",
  },
  {
    title: "Lens Loop",
    description:
      "保存からインサイト提供まで数秒。Docker/K8s 上でバックグラウンド検証を回し続ける、究極の開発ループ。",
    icon: "🔁",
    color: "var(--color-aether-accent)",
  },
  {
    title: "MCP Synergy",
    description:
      "MCP 経由で AI エージェントとリアルタイム同期。階層化されたコンテキストにより、知性を極限まで加速させます。",
    icon: "🤝",
    color: "#8b5cf6",
  },
];
---

<div class="features-grid">
  {
    features.map((feature) => (
      <div class="feature-card" style={`--accent-color: ${feature.color}`}>
        <div class="icon-wrapper">{feature.icon}</div>
        <h3>{feature.title}</h3>
        <p>{feature.description}</p>
        <div class="glow" />
      </div>
    ))
  }
</div>

<style>
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    padding: 2rem 0;
  }

  .feature-card {
    position: relative;
    background: var(--color-surface-glass);
    border: 1px solid var(--border-glass);
    border-radius: 1.5rem;
    padding: 3rem 2rem;
    overflow: hidden;
    transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    cursor: default;
  }

  .feature-card:hover {
    transform: translateY(-12px);
    border-color: var(--accent-color);
    background: rgba(var(--accent-color), 0.02);
    box-shadow: 0 40px 80px -20px rgba(0, 0, 0, 0.4);
  }

  .icon-wrapper {
    font-size: 3.5rem;
    margin-bottom: 1.5rem;
    filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.1));
    transition: transform 0.5s ease;
  }

  .feature-card:hover .icon-wrapper {
    transform: scale(1.2) rotate(5deg);
  }

  h3 {
    font-size: 1.75rem;
    font-weight: 800;
    margin-bottom: 1rem;
    color: var(--sl-color-white);
  }

  p {
    font-size: 1.125rem;
    line-height: 1.6;
    color: var(--sl-color-gray-3);
    margin: 0;
  }

  .glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(
      circle at 50% 0%,
      var(--accent-color),
      transparent 70%
    );
    opacity: 0;
    transition: opacity 0.5s ease;
    pointer-events: none;
    z-index: -1;
  }

  .feature-card:hover .glow {
    opacity: 0.1;
  }

  @media (max-width: 768px) {
    .feature-card {
      padding: 2rem 1.5rem;
    }
  }
</style>
